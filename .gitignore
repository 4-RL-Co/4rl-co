# build & deps
node_modules/
dist/
.astro/
.vercel/
.output/

# env: ignore every env file, but keep the documented template
.env
.env.*
!.env.example

# secrets / keys (never commit these)
*.pem
*.key
*.p12
*.pfx
id_rsa*
*.keystore
secrets.json
.secrets/

# os / editor / logs
.DS_Store
Thumbs.db
npm-debug.log*
yarn-error.log*
*.local

# personal notes
Como usar o mark (Claude Code).md
